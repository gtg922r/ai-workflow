# Runner Ralph Dependencies
#
# NOTE: If using `uv run ralph.py`, dependencies are managed automatically
# via inline script metadata (PEP 723). This file is kept for compatibility
# with traditional pip workflows.

# TUI Framework
textual>=0.47.0

# Rich text formatting (dependency of textual, but explicit for clarity)
rich>=13.0.0

# Development dependencies (optional)
# pytest>=7.0.0
# mypy>=1.0.0
# ruff>=0.1.0
